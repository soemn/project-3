  <div class="card">
  <%= cl_image_tag @photo.photo_link, class:"card-img-top"  %>
    <div class="card-body">
        <h4 class="card-title">
          <%= @photo.title %>
        </h4>
          <p class="card-text"><%= @photo.description %></p>
          <p class="card-text">Points : <%= @photo.points %></p>
      </div>
    </div>
